# 数据处理记录

## 概要

本次处理完成了两项工作：
1. 将专家预测原始数据转换为与现有拥堵数据相同的CSV格式
2. 更新2025年全年真实拥堵数据

---

## 1. 专家预测数据处理

### 背景

实验此前缺少与专家预测结果的对比。新增的专家预测数据位于 `data/prediction_data/`，包含3个Excel文件，格式为NEXCO支社结合用エラーチェック様式（107列），与现有拥堵数据格式完全不同，需要转换。

### 输入文件

| 文件 | 月份 | 记录数 |
|------|------|--------|
| `【確定】R7年4月_N東【支社結合用エラーチェック様式】.xlsx` | 2025年4月 | 658条 |
| `【確定】R7年5月_N東【支社結合用エラーチェック様式】.xlsx` | 2025年5月 | 641条 |
| `【確定】R7年8月_N東【支社結合用エラーチェック様式】.xlsx` | 2025年8月 | 971条 |

### 字段映射

| 专家数据字段 | 目标字段 | 转换逻辑 |
|---|---|---|
| `月日` (datetime) | `date` | 格式化为 YYYY-MM-DD |
| `方向` ("上り"/"下り") | `上下` | "上り"→"上", "下り"→"下", "内回り"→"内", "外回り"→"外" |
| 固定值 | `原因` | 始终为 "交通集中" |
| `道路名` ("関越自動車道") | `道路番号` | 映射为简称（"関越道"等，共22条路线） |
| `時間帯` ("9:00～13:00") | `発生時刻` | 取时间范围的开始时间 → "09:00:00" |
| `時間帯` ("9:00～13:00") | `渋滞時間` | 从时间范围计算时长 → 240分钟 |
| `ピーク時間` ("10時" / 10) | `ピーク時刻` | 统一解析为 "10:00:00" |
| `ピーク時の渋滞長` | `ピーク長` | 直接使用（已是km单位，不需÷10） |
| `KP` | `発生Ｋｐ` | 直接使用（已是km单位） |
| 固定值 | `発生時渋滞長` | 设为 0.0（专家数据无此信息，生成的图形均为三角形） |

### 特殊处理

- **Excel结构**: 使用 `header=1` 读取（Row 0为标题行），列名含换行符需清洗（`ピーク\n時間` → `ピーク時間`）
- **跨日时间**: "15:00～翌0:00" → 発生時刻=15:00:00, 渋滞時間=540分钟
- **峰值时间格式混合**: 字符串 "10時"、整数 10、"翌0時" 均统一解析

### 输出

- **处理脚本**: `process_expert_prediction_data.py`
- **输出目录**: `data/processed_prediction_data/`
- **汇总文件**: `all_prediction_data.csv`（2,270条记录）
- **分日CSV**: 1,242个文件，命名格式 `{road}_{direction}_{year}_{month}-{day}.csv`

### 统计

```
总记录数: 2,270
日期范围: 2025-04-01 ～ 2025-08-31

路线分布 (Top 10):
  外環道:      470
  京葉道路:    401
  東北道:      243
  圏央道:      217
  常磐道:      209
  関越道:      207
  東関東道:    113
  アクアライン:  94
  横横道路:     66
  横浜新道:     64

方向分布:
  上: 1,011 / 下: 553 / 外: 383 / 内: 304 / 西: 19
```

---

## 2. 2025年真实拥堵数据更新

### 背景

新增了2025年全年拥堵数据文件 `★2025_関東支社渋滞データ（01-12）SIC分割【コード変換・BT記入・本社BT】特定更新工事-緊急工事.xlsx`，需要合并到 `data/processed_data/` 中。

### 处理

使用与 `process_real_congestion_data.ipynb` 相同的处理逻辑重新运行：

1. 读取 `data/meta_data/` 下所有Excel文件（2014-2025）
2. 转换时间格式（数值 → HH:MM:SS）
3. 数值字段÷10（ピーク長、発生Ｋｐ、発生時渋滞長）
4. 过滤原因="交通集中"
5. 去重（基于全部关键列）
6. 按 date/road/direction 分组生成CSV

### 结果

| 指标 | 更新前 | 更新后 |
|------|--------|--------|
| all_data.csv 总记录数 | 221,868 | **224,957** |
| 分日CSV文件数 (東北道+関越道) | ~7,693 | **7,766** |
| 2025年関越道 | 部分（1-11月） | **1,919条**（1-12月） |
| 2025年東北道 | 部分（1-11月） | **1,817条**（1-12月） |

---

## 3. 验证

### 格式一致性

专家预测CSV与真实拥堵CSV列结构完全一致：

```
date, 上下, 原因, 道路番号, 発生時刻, ピーク時刻, ピーク長, 発生Ｋｐ, 発生時渋滞長, 渋滞時間
```

### 几何图形生成

专家预测数据可被 `workflow/functions.py` 中的 `generate_direction_aware_polygons()` 正常处理，生成三角形图形。

示例（2025-05-05 関越道 上行）：

```
[0] triangle: kp=[0.0, 5.0],   time=[900, 1140]   (15:00-19:00)
[1] triangle: kp=[21.2, 31.2], time=[900, 1200]    (15:00-20:00)
[2] triangle: kp=[33.0, 73.0], time=[720, 1500]    (12:00-翌1:00, 峰值40km)
[3] triangle: kp=[75.5, 80.5], time=[660, 1140]    (11:00-19:00)
[4] triangle: kp=[95.7, 105.7], time=[660, 1200]   (11:00-20:00)
```

### 数据对比示例 (2025-05-05 関越道 上行)

| 数据源 | 记录数 | 说明 |
|--------|--------|------|
| 真实数据 | 25条 | 细粒度事件，含初始渋滞長 |
| 专家预测 | 5条 | 粗粒度热点，按瓶颈位置汇总 |

专家预测的5个热点位置（KP=0, 21.2, 33.0, 75.5, 95.7）与真实数据中的高频拥堵位置高度吻合。

---

## 文件结构

```
data/
├── meta_data/                       # 原始NEXCO Excel (2014-2025)
│   └── ★2025_...(01-12)...xlsx     # [新增] 2025全年数据
├── prediction_data/                 # 专家预测原始Excel
│   ├── 【確定】R7年4月_N東...xlsx
│   ├── 【確定】R7年5月_N東...xlsx
│   └── 【確定】R7年8月_N東...xlsx
├── processed_data/                  # 真实拥堵数据 (已更新)
│   ├── all_data.csv                # 224,957条
│   └── {road}_{dir}_{year}_{m-d}.csv
└── processed_prediction_data/       # [新增] 专家预测数据
    ├── all_prediction_data.csv     # 2,270条
    └── {road}_{dir}_{year}_{m-d}.csv

process_expert_prediction_data.py    # [新增] 专家预测处理脚本
process_real_congestion_data.ipynb   # 真实数据处理notebook (已有)
```

---

## 后续使用

在evaluation模块中，可以从 `data/processed_prediction_data/` 加载专家预测数据，使用现有的评估指标（Polygon IoU、Grid F1、Layered IoU）进行三方对比：

- **LLM预测 vs 真实数据**（已有）
- **专家预测 vs 真实数据**（新增）
- **LLM预测 vs 专家预测**（新增）
